<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cat.copernic.jose.antonio.miranda.prodiscomtest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProdisComTest">
        <activity
            android:name=".ui.app.register.ConRegistro"
            android:exported="false" />
        <activity
            android:name=".ConfRegsitro"
            android:exported="false"
            android:label="@string/title_activity_conf_regsitro"
            android:theme="@style/Theme.ProdisComTest.NoActionBar" />
        <activity
            android:name=".ui.app.register.Register"
            android:exported="false" />
        <activity
            android:name=".ui.app.logged.MainActivity"
            android:exported="false" />
        <activity
            android:name=".ui.app.LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/splashTeme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>