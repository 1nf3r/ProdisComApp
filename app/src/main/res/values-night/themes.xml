<resources xmlns:tools="http://schemas.an0droid.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ProdisComTest" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/prodis</item>
        <item name="colorPrimaryVariant">@color/prodis</item>
        <item name="colorOnPrimary">@color/purple_500</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/grayDark</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@color/grayDark</item>
    </style>

    <style name="Theme.ProdisComTest.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="Theme.ProdisComTest.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.ProdisComTest.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>